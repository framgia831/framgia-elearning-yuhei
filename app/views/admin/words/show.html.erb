<h1 class="text-center">Edit</h1>
	<div class="row">
		<div class="col">
			<%= @word.content %>
		</div>

		<div class="col">
			<% @choices.each do |choice| %>
				<%= choice.content %>
			<% end %>
		</div>	
	</div>	